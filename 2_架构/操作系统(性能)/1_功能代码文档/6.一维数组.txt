变量：
	
	定义变量：分配一个元素空间
	初始化变量：
		在定义同时赋值
	引用变量：引用空间的内容
	  PS:引用时必须先定义
	
	
	
数组：一块“连续”且同类型的空间
定义数组：
	类型 数组名[长度]; //这个可以不初始化
	类型 数组名[]={...};//必须初始化，由于初始化元素来决定数组的长度
   PS:分配的空间长度   sizeof(类型)*长度
	  数组名：是首地址，常量

初始化：在定义数组同时赋值 
	PS:只初始化部分值时，未被初始化的元素值为0
	
	
引用：不能整体引用，只能一个一个元素地引用
	数组名[下标];   
	PS：下标是从0开始的
	
	
PS：在定义数组的时必须初始化，否则只能一个一个的赋值（因为数组名是首地址）
	int buf[10];
	buf={1,2,3,4};错误的写法
	
	
	
练习：
	1、输出数组中每一个元素
	   倒置输出数组中每一个元素
	2、求和
	3、求最大值/最小值
	4、输出偶数值
	4、打印下标为奇数的元素值
累加：
		容器count+=值
	
	
	
今天晚上：
		复习：
		1、昨天的循环题目再做一遍。（博一博）
		2、把今天的数组的知识点（课堂上的模块再自己做一遍）
练习题：
		1、定义一个整型数组并初始化
			1、该数组分配空间长度
			2、倒置输出
			3、元素之和
			4、求平均值
			5、输出比平均值大的元素值
			
		2求二进制（例用数组）
		
		
默写题目：
		1、监哨值法(最值)
		
		
		2、选择排序
	
	
	
		
	
	
	
			