复习：
	1、linux的特点：
		1、"免费"  开源：内核代码公开（C语言）
			鸿蒙系统（基于Linux) 
		2、稳定性强：服务器
		3、内核可裁剪：嵌入式  （最小可达到128kb）		
		4、移植性强：
		5、"多用户"多任务操作系统

虚拟机的快捷方式：
	1、切换光标：ctrl+alt
	2、进入/退出全屏模式（占据整个屏幕）：	
			1、GUI: 菜单栏--》"进入全屏模式"按钮
			2、Ctrl-ALT  +   回车
	3、熟练Linux：Fedora	(用户习惯跟IOS/MAC系统是类似的)
	   修改屏幕的分辨率，保持跟物理屏幕的分辨率一致。
	   
（目前）定的目标工作：运维
Shell命令的学习：
	0.Linux运行的工作环境：终端
	  winodws系统运行环境：cmd
	1、shell命令：就是一串操作电脑的特定的字符串 (见命知其义） 
		1、查看当前的终端用户(我是谁）
			who am i
		2、查看工作目录：(用户在终端所处的位置）
		   pwd 			
		3、创建文件夹：	
		   mkdir  文件夹名	
		   创建文档：
		   touch  文件名
		   
		   删除： remove
		     删除文件名
				rm  文件名  (默认的删除文件）
			 删除文件夹
				rm -rf 文件名
		   
		4、帮助手册：man 手册
			man 命令/系统函数
			q退出man手册 
		5、改变工作目录  change   director
		   cd  目录
			
		6、遍历路径的内容：
			ls [-arg]  路径
			1、 ls   不加路径时  （遍历工作目录的内容）
			2、 ls  路径 
			3、 ls -a  路径
			4、 ls -l 文件
				ls -l ./a.txt
				-rwxrwxrwx 1 LF  LF   12 3月 26 09：55 ./a.txt
				文件的类型
					
		注：为了区分各种文件类型，用不同颜色来代表  蓝色--文件夹   黑色---文件	
		
		7查看文本文档：将文档中的内容显示到屏幕上
				cat   一次性显示文件的内容
				more  分屏显示文件的内容
			
		8、过滤：过滤用户感兴趣的信息。grep  
			grep  -i  key   文件中
		
		9、统计文档的信息： 统计单词，字节（长度），行数，。。。
				wc  -l  行  -w单词  -c字符  -b 长度
				
				
		10、重定向：将文件的输入输出重新改变它的输入输出流
					>   截断重定向			截断：每次重定向时，都截断为0则写入		（覆盖）
					>>  追加重定向			追加：从文件的末尾添加内容				（追加）					
		
		11、管道（*）：作用多条命令的”连接器”  （内核）
			command1 | command2 | command3 |....
			注：1、执行顺序从左往右执行
				2、将上一个Shell的输出到屏幕上的结果作为下一个Shell命令输入内容
		
		12、拷贝文件				copy拷贝 
				cp  源文件  目标文件
			移动文件/重命令：			move 
				mv  源路径    目标路径
				注：同层路径为重命名   不同层为移动
			拷贝文件夹：
				cp -r  源文件夹  目标文件夹
				cp  -a 源文件夹  目标文件夹
	
		
		
		13、用户与用户组
			Linux特点：多任务多用户操作系统（可以有多个任务同时执行，可以允许多个用户同时登录使用）
				用户：
					1、创建用户:useradd 用户名
							注：普通用户不能创建与删除用户，root才拥有创建与删除的权限
						注：用户的信息保存在/etc/passwd	
							LF:x:501:502:COUNT:/home/LF:/bin/bash
							用户:密码:用户ID:群ID:备注:用户的目录:终端
							注：密码采用MD5值加密
						
					2、删除用户：userdel -rf userName
					3、切换用户:(切换用户进栈：保存到数组中)
							su 用户名
							注：输入密码时是以非回显方式输入（不显示）
						退出用户：
							exit
					4、修改密码：passwd  用户名	
						注：自己修改自己的，或者是超级管理员
					5、删除用户：
						userdel 用户名			注：只会删除用户的记录，但不会删除用户的信息
						userdel -rf 用户名 注：删除用户不仅将用户的记录删除掉，还要删除该用户的所有信息
					注：1、注销：就是把该用户退出登录
						2用户分类：
							1、超级用户:具备有一切的权限的用户  root
							2、普通用户:非超级用户以外其他可登录使用的用户
				用户组：一组同类权限的用户集（3月31号）  
					1、创建用户组：				申请一个QQ群
						groupadd  组名
						注：用户组的信息保存在/etc/group
							developer:x:506:成员列表
							组名:x:组ID:成员列表
					2、将指定用户添加到群组中
						usermod -a   -G 组ID    用户ID   
								添加  组  		用户ID
						将指定用户从组中删除：		
						gpasswd -d 组名  用户名   
					3、删除用户组:
		
		14、权限（3月31号）：
			1、权限：一个文档对于用户操作的一种许可
					读操作：r	4
					写操作：w   2
					执行:   x   1
					
					创建者       群组	其他人
					rwx 	 rwx  	rwx
				二进制：	110	 110         110	注：对应位为1时，对应的操作就具体对应的权限
				八进制：	6       	 6      	6			
					
		    2、三种用户： 	  	创建者U   ：创建该文档的人
					 群组成员G  Group：一组同类权限的用户集
					其他用户O   Other：陌生人
		
				查看用户权限：
						ls -l   a.txt
						-   rw- rw-  rw-. 1 LF  LF   	a.txt
						类   创 群   其     创  群  	 文
						型   建 组   他     建  组	 件
							者     者  名                名
												名
							r-- ---  ---					\
												
			3、修改权限：
				chmod   用户 +/- 权限,用户+/-权限,  文件名		 
					注：  +添加   -去掉
				eg：  chmod u-r a.txt   翻译：对于a.txt来进行删除U(创建者)的r(读)的权限
						
						
			4、文件的掩码：（*服务器开发中，必须用到此技术）
				创建文件时：以固定的权限来创建文件
					
				新创建的文件权限：默认的权限为666-文件的掩码，才为该创建文件的权限
				
				(创建的文件的权限=默认的创建权限666(被减数)-掩码(减数))
				1、查看当前终端的文件掩码
					umask
				2、设置掩码
					umask  掩码值
				eg:在0331的文件夹中，新创建的文档的权限都为: 110 000 000
					步骤1：设置文件掩码 066
					步骤2：创建文件
				
		8（*）、vi编辑器：一款命令丰富且优秀的命令行的文本编辑器	
			1、打开：   vi  文件名
			
			
			2、vi的模式：
				1、插入模式：插入字符
					1、命令模式：ESC
				2、命令模式：命令操作，复制，粘贴，查找、替换等等。
					1、进入vi时，在命令模式
					2、复制： yy
					3、切换到插入模式：
						i/I    i光标行的左侧   I光标行最左侧
						a/A    a光标右侧写入   A最右侧
						o/O   o下行           O上一行

						
					4、 切换到底模式：
						shift+:
					5、复制
						nyy    n数字（复制多行：nyy ， 复制一行：yy）
					6、粘贴：
						p
					7、剪切：
						ndd
					8、撤销：
						u
					9、恢复：
						ctrl+r
					10、替换
						r  替换一个字符
						R 连续的替换（ESC取消  退格键删除替换字符）
					11、查找字符：
						/字符串    n向下查出下一个字符串的位置
						?字符串	  n向上查出下一个字符串的位置	
				3、底行模式：保存 退出
					q 直接退出 (不能有修改)
					w 保存
					wq 保存退出
					q! 强制退出
					set nu 显示行号
					set nonu 取消行号
			注：1、区分大小写	
				2、执行Shell命令
					命令  arg1  arg2  arg3  arg4  arg5....
					ls  -a -l ./
	
	2、linux的文件系统。
		1、Linux与windows系统的文件系统是不同的系统
		   windows:     C:\Users\lx\Desktop\C++的设置模式.txt
						逻辑盘：C  D   E ...
						C:\文件夹\文件			\父子文件分隔符
						
		   linux:       /home/LF/桌面/a.txt 	
					从根开始的/     其他的/代表为分隔符
		2、工作目录：当前所在的位置
			(凡是没有指明绝对路径的，所代表的路径就是为工作目录）
			用户主目录：系统为每个用户创建一个私有的文件夹作为该用户的存储信息的容器，
						/home/LF  通过~来替代
					
		3、相对【工作目录】路径：
				相对你所在位置而言的路径(相对于工作目录而言)
					（你在的位置：）5栋302
		   绝对路径：从(起始地址)根开始表示的路径
			  中国-湖北省--武汉市--江夏区--金融港B27
			..代表的是父目录（上一层目录）
			.代表的是当前目录
			
		4、linux的终端 		具有命令补充功能：输入一个命令的一部分时或者路径名的一部分会自动补齐(tab补齐)
			注：如果出现歧义，它不会补齐。但是再次点击tab键时，会以列表的显示提示
			
	
			
			
练习：
	1、在桌面创建一个以自己名字命名的文件夹
	2、在该文件夹下创建一下lf.txt的文档
	3、删除该lf.txt的文档
	
	
练习：
	1、明天找所有学生默写回答
	2、在用户主目录中创建study的文件夹
       2.1在study文件中创建一个a.txt文档
	   2.2写入一些内容
	   2.3输出a.txt文档的内容
	   2.4查找指定信息的内容
	   2.5统计该文档：有多少行，多少单词的数量

练习：	
	1、输出统计某路径下的文件的个数
	2、输出   统计文档文件中出现某关键字的次数
	3、拷贝文件夹


预科：很重要的
  1、>60  		（理解学校的课程）   
  
练习：	
	1、不允许创建者修改该（写入）文档的内容
	2、创建lf.txt的文档
		1、删除lf.txt 读的权限   统计该文档的单词的数量，若不能统计数量，则应该怎么办？
	3、创建一个帐号，并在该用户的目录中创建一个0331的文件夹



练习：
	1、将a.txt的权限修改为：创建者：只读  群组没有权限  其他人：不具备权限
		二进制：100 000 000
		八进制：4    0  0
	2、再将a.txt的权限修改为：所有用户都具有r、w不可执行

	3、在桌面上创建study的文夹下，在该文件夹中创建文件时，默认权限为： 创建者：读- ，群组：读-- 其他人：读--






练习：(完成了学习Shell脚本编程时)
	1、200个学生，请创建200个帐号


	
			